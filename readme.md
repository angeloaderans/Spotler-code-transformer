* Spotler code transform
**** This executable will transform a by Spotler generated form (list) to proper HTML.

Put the by Spotler generated HTML code in a HTML file called form.html, and run the executable.
